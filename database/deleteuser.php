<?php
    // require_once 'config.php';
    
    // $id = $_POST['id'];

    // $query = "UPDATE `projects` SET `status` = 'deleted' WHERE `id` = :id; ";
    // $statement = $con->prepare($query);
    // $removed = $statement->execute(
    //     array(
    //         ':id'               =>  $id, 
    //     )
    // );

    // if($removed){
        echo "success";
    // }
    // else{
    //     echo "Something went wrong";
    // }
?>